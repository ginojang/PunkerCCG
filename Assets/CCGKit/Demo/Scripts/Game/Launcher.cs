using UnityEngine;


public class Launcher : MonoBehaviour
{
    private void Awake()
    {
        CCGKit.GameManager.Instance.Initialize();

    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
